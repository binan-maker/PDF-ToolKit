# DISABLED - Merged into deploy.yml
# This workflow has been merged into deploy.yml for sequential execution
# All builds (AAB + PlayStore APK + OpenSource APK) now happen in deploy.yml
#
# Previous content moved to deploy.yml which now handles:
# 1. Build AAB (for Play Store)
# 2. Build PlayStore APK  
# 3. Build OpenSource APK
# 4. Deploy to Play Store
# 5. Deploy to Indus
# 6. Create GitHub Release with ALL files

name: Disabled - Merged into Deploy

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow Disabled
        run: |
          echo "⚠️ This workflow has been merged into deploy.yml"
          echo ""
          echo "All builds now happen sequentially in the unified deploy workflow:"
          echo "  - AAB (Android App Bundle)"
          echo "  - PlayStore APK"
          echo "  - OpenSource APK"
          echo ""
          echo "See .github/workflows/deploy.yml for the unified workflow"
          echo ""
          echo "To trigger a release, push to master or run deploy.yml manually"
