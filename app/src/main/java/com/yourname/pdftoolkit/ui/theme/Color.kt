package com.yourname.pdftoolkit.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * Paperly palette
 *
 * The home screen uses a warm editorial canvas with a coral/violet accent
 * system. Keeping the same tokens in MaterialTheme means tool screens feel
 * like the next page of the same product instead of a default Material demo.
 */
val PaperlyCoral = Color(0xFFE85D4A)
val PaperlyCoralBright = Color(0xFFFF806D)
val PaperlyViolet = Color(0xFF6653C7)
val PaperlyTeal = Color(0xFF008A7A)
val PaperlyInk = Color(0xFF24212B)
val PaperlyCanvas = Color(0xFFF7F4EF)
val PaperlySurface = Color(0xFFFFFCF8)
val PaperlySurfaceVariant = Color(0xFFEEEAE4)
val PaperlyOutline = Color(0xFFD8D0C7)

val PaperlyDarkCanvas = Color(0xFF121116)
val PaperlyDarkSurface = Color(0xFF1C1A21)
val PaperlyDarkSurfaceVariant = Color(0xFF2A2730)
val PaperlyDarkInk = Color(0xFFF8F5F2)
val PaperlyDarkOutline = Color(0xFF4A454F)